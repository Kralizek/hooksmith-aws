{
  "workspace": [
    "./packages/aws",
    "./packages/aws-lambda"
  ],
  "imports": {
    "@hooksmith/core": "jsr:@hooksmith/core@^0.7.0",
    "@hooksmith/runtime": "jsr:@hooksmith/runtime@^0.7.0",
    "@std/assert": "jsr:@std/assert@^1"
  },
  "minimumDependencyAge": 0,
  "tasks": {
    "check": "deno fmt --check && deno lint && deno test -A && deno check packages/*/mod.ts",
    "test": "deno test -A"
  },
  "lock": false
}
