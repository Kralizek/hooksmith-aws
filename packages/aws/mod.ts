/**
 * Shared AWS integration types for Hooksmith.
 *
 * Import service integrations from the corresponding subpath, such as
 * `@hooksmith/aws/sqs` or `@hooksmith/aws/eventbridge`.
 *
 * @module
 */

export type { AwsEnrichmentMap } from "./shared/enrichment.ts";
export type { ValueOrFactory } from "./shared/value.ts";
